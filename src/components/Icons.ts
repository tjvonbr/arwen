import { Cable, Code2, Terminal, type LucideIcon } from "lucide-react";

export interface Icons {
  [key: string]: LucideIcon;
}

export const Icons: Icons = {
  cable: Cable,
  code2: Code2,
  terminal: Terminal,
};
